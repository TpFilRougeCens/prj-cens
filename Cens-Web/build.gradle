apply plugin: 'war'

version = '1.0.0-SNAPSHOT'
webAppDirName = 'web'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
            srcDir 'src'
        }
    }
}

dependencies {
    compile project(':Cens-Ejb')
    compile group: 'javax', name: 'javaee-api', version: '7.0'
}