version = '1.0.0-SNAPSHOT'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
            srcDir 'src'
        }
    }
}

dependencies {
    compile group: 'javax', name: 'javaee-api', version: '7.0'
}